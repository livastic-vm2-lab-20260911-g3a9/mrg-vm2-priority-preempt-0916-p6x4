merge_queue:
  mode: serial

commands_restrictions:
  queue:
    conditions:
      - sender = {{author}}

queue_rules:
  - name: VM2 per-PR command restriction isolation
    batch_size: 1
    queue_conditions: []
    merge_conditions: []
    merge_method: merge

pull_request_rules: []
